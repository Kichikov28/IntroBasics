using System;

namespace P02_TrekkingMania
{
    class Program
    {
        static void Main(string[] args)
        {
            int groups = int.Parse(Console.ReadLine());
            for (int i = 0; i < groups; i++)
            {
                int peopleinGroups = int.Parse(Console.ReadLine());
                if (peopleinGroups<=5)
                {
                    
                }
            }
        }
    }
}
